# Dotfiles

# Install
Clone the repo as `Dotfiles` in to `$HOME`

```
cd $HOME
git clone git@github.com:patrickheeney/dotfiles.git Dotfiles

cd Dotfiles
./install-dotfiles.sh
./install-homebrew.sh
```
